services:
  gemini_cli:
    build: .
    container_name: gemini_cli
    volumes:
      - .:/workspace
      - ~/.netrc:/root/.netrc
      - ${GEMINI_WORKSPACE_DIR}:/app
    env_file:
      - .env
    networks:
      global-net:

networks:
  global-net:
    external: true
